version: "2"

services:
    db:
       image: postgres
       restart: always
       environment:
         POSTGRES_USER: postgres
         POSTGRES_PASSWORD: Tmr6nL22PfXxJgh
       ports:
         - 5432:5432
       volumes:
         - "postgres:/var/lib/postgresql/data"

    server:
       build: .
       ports:
         - "9995:9995"
       depends_on:
         - db
    volumes:
	db:
